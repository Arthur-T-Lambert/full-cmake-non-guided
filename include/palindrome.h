#ifndef PALINDROME_H
#define PALINDROME_H

/**
 * @file palindrome.h
 * @brief Check if a string is a palindrome.
 *
 * @param str Char* The character string.
 * @param len Integer The length of the character string.
 * @return Bool True if the string is a palindrome, false otherwise.
 */
int palindrome(int a);

#endif // PALINDROME_H